export interface NavbarItem {
  title: string;
  path: string;
}

export interface Project {
  slug: string;
  image: string;
  title: string;
  description: string;
  status: string;
  location: string;
  services: string;
}
